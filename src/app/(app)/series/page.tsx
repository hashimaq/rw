import { PlaceholderPage } from "@/components/pages/placeholder-page";

export const metadata = { title: "Series" };

export default function SeriesPage() {
  return (
    <PlaceholderPage
      title="Series"
      description="Series are stored in Supabase. Management and listings will expand once real series records exist."
    />
  );
}
