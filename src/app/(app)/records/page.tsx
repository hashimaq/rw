import { PlaceholderPage } from "@/components/pages/placeholder-page";

export const metadata = { title: "Records" };

export default function RecordsPage() {
  return (
    <PlaceholderPage
      title="Records"
      description="Team records will be calculated from completed matches. No placeholder records are shown."
    />
  );
}
