import { PlaceholderPage } from "@/components/pages/placeholder-page";

export const metadata = { title: "Stats" };

export default function StatsPage() {
  return (
    <PlaceholderPage
      title="Stats"
      description="Statistics will be derived from real match data using the existing statistics foundation."
    />
  );
}
