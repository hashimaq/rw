/** Local scorecard background track — replace `public/audio/scorecard-music.mp3` as needed. */
export const SCORECARD_MUSIC_SRC = "/audio/scorecard-music.mp3";

/** Background level (0–1); not muted — user mute is separate. */
export const SCORECARD_MUSIC_VOLUME = 0.38;
