# Scorecard background music

The scorecard uses:

`public/audio/scorecard-music.mp3`

(Sourced from the project’s Champions Trophy scorecard music file, copied here for a stable URL.)

The full match scorecard route (`/match/[slug]`) loads this asset automatically. Replace this file to change the loop. The scorecard still works if the file is missing.
